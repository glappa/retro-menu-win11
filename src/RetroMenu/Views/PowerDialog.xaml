<Window x:Class="RetroMenu.Views.PowerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Retro Menu"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        FontFamily="{DynamicResource MenuFontFamily}"
        FontSize="{DynamicResource MenuFontSize}">

    <Window.Resources>
        <Style x:Key="OrbButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Margin" Value="14,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="4" Padding="8,6">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ClassicButton" TargetType="Button">
            <Setter Property="Padding" Value="16,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="#FFECECEC" BorderBrush="#FF7B7B7B"
                                BorderThickness="1" CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FFFFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource MenuBorderBrush}" BorderThickness="1"
            CornerRadius="8" Background="{DynamicResource FooterBackground}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="14" ShadowDepth="4" Direction="315" Opacity="0.5" />
        </Border.Effect>

        <Grid Width="430">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource HeaderBackground}"
                    CornerRadius="7,7,0,0" Padding="14,10">
                <TextBlock x:Name="TitleText" FontWeight="Bold" FontSize="16"
                           Foreground="{DynamicResource HeaderForeground}" />
            </Border>

            <StackPanel Grid.Row="1" x:Name="Choices" Orientation="Horizontal"
                        HorizontalAlignment="Center" Margin="0,18,0,14" />

            <Border Grid.Row="2" Background="#22000000" CornerRadius="0,0,7,7" Padding="12,8">
                <Button x:Name="CancelButton" Style="{StaticResource ClassicButton}"
                        HorizontalAlignment="Right" Click="OnCancel" />
            </Border>
        </Grid>
    </Border>
</Window>
